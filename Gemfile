source "https://rubygems.org"

# Specify the version of Jekyll you want to use
gem "jekyll"

# For getting my publications
gem "jekyll-scholar"

# Add the minima theme (assuming you are using the default theme)
gem "minima"

# If you need to use the jekyll-feed, jekyll-sitemap, or jekyll-seo-tag gems
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
  gem "jekyll-seo-tag"
  # Add any other necessary plugins here
end

# NOTE: If you are hosting on GitHub Pages, 
# it's usually better to use the github-pages gem:
# gem "github-pages", group: :jekyll_plugins
